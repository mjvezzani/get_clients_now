<nav class='navbar navbar-default' role='navigation'>
  <div class='container-fluid'>
    <div class='navbar-header'>
      <% if !logged_in? %>
        <%= link_to 'Conte', root_path, class: 'navbar-brand' %>
      <% else %>
        <%= link_to 'Conte', dashboard_path, class: 'navbar-brand' %>
      <% end %>
    </div>
    <div class='btn-group nav-item pull-right'>
        <% if logged_in? %>
          <%= link_to 'New Client', new_client_path, class: 'btn navbar-btn btn-info' %>
          <%= link_to 'Log Out', logout_path, class: 'btn navbar-btn btn-info' %>
        <% else %>
          <%= link_to 'Log In', login_path, class: 'btn navbar-btn btn-info' %>
        <% end %>
    </div>
  </div>
</nav>
